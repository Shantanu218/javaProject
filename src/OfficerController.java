public class OfficerController {
    private BTOProject project = new BTOProject();
    private HDBManager manager = new HDBManager();

    public String applyForProject(OfficerData officerData) {
        
        return "";
    }

    public String registerAsOfficer(OfficerData officerData) {
//        checkRegistrationEligibility(officerData)
//        requestRegistrationApproval(officerData)
//        updateRegistration(officerData)
//        displayRegistrationStatus()

        return "";
    }

    private boolean updateRegistration(OfficerData officerData, String status) {
        
        return false;
    }
}
